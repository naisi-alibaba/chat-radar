# ======================================================================
#  环境变量样板文件（.env.example）
# ----------------------------------------------------------------------
#  这个文件是什么？
#    它是「密钥和环境变量的清单模板」。本文件可以安全提交到 git，
#    因为它只列出有哪些变量、不含真实值。
#
#  怎么用？
#    1) 把本文件复制一份，命名为  .env
#    2) 在 .env 里填入真实的值
#    3) .env 已被 .gitignore 忽略，不会上传，可放心存放密钥
#
#  下面每个变量上方的 # 注释，说明它是干什么的、从哪里获取。
#  用到哪个就取消注释（删掉行首的 #）并填值，没用到的留着即可。
# ======================================================================

# 运行环境：development / production
# NODE_ENV=development

# 服务监听端口
# PORT=3000

# --- 示例：第三方服务的 API Key ---
# 从该服务后台 → 开发者设置 → API 密钥 处获取
# API_KEY=

# --- 示例：飞书应用凭证 ---
# 来源：飞书开放平台 → 你的应用 → 凭证与基础信息
# FEISHU_APP_ID=
# FEISHU_APP_SECRET=

# ======================================================================
#  chat-radar 实际需要的变量（v0.1）
# ----------------------------------------------------------------------
#  飞书：v0.1 直接复用本机 lark-cli 的用户授权，无需在此填飞书凭证。
#        确认授权状态：lark-cli auth status
#  DeepSeek：裁定用的大模型 key，从 https://platform.deepseek.com 获取。
# ======================================================================

# DeepSeek API Key（裁定引擎，必填）
DEEPSEEK_API_KEY=

# 可选：覆盖 DeepSeek 接口地址 / 模型
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
